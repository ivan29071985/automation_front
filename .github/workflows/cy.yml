name: Cypress CI/CD

on:
  push:
    branches:
      - main
      - master
      - develop
  pull_request:
    branches:
      - main
      - master
      - develop

jobs:
  cypress-run:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      # Passa a mensagem do commit e já imprime no log
      - name: Set commit message
        run: |
          echo "GIT_COMMIT_MESSAGE=${{ github.event.head_commit.message }}" >> $GITHUB_ENV
          echo "Commit message is: ${{ github.event.head_commit.message }}"

      # Rodar Cypress com Allure
      - name: Run Cypress tests with Allure
        run: npm run cypress:run:allure

      # Upload do relatório Allure
      - name: Upload Allure Report
        if: success() || failure()
        uses: actions/upload-artifact@v3
        with:
          name: allure-report
          path: allure-report
